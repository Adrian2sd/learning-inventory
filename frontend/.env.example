# .env.example — Copia como .env.local
# En producción (Vercel), configura esto en las variables de entorno del proyecto

# URL del backend (en producción, será tu URL de Vercel del backend)
# En desarrollo, el proxy de Vite lo maneja automáticamente → dejar vacío
VITE_API_URL=
